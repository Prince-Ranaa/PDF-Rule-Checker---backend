🚀 Setup Instructions

1. Clone the project
git clone https://github.com/Prince-Ranaa/PDF-Rule-Checker---backend.git

cd PDF-Rule-Checker---backend
or
cd to the folder



2. Install dependencies
npm install

3. Create a Groq account

Signup here:
https://console.groq.com/home

Generate your API Key.



4. Create a .env file

Add the following inside it:

GROQ_API_KEY="your_groq_api_key"



5. Run the server
npm run dev



Server will start at:

http://localhost:5000

⚠️ Important

To use this backend correctly, make sure the server is running locally before using the frontend.
